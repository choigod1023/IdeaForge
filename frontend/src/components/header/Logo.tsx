import { headerStyles } from "../../styles/headerStyles";

export const Logo = () => (
  <div className={headerStyles.logo.container}>
    <div className={headerStyles.logo.icon}>IF</div>
    <h1 className={headerStyles.logo.text}>IdeaForge</h1>
  </div>
);
